import AICorePage from "@/features/dosreb-ai-core/ui/AICorePage";

export default function AICoreAdminPage({ params }: { params: { locale: string } }) {
  // You can use params.locale for localized logic if needed
  return <AICorePage />;
}